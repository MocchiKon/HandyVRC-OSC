# Filled in by Maven at build time (see the filtered resources in pom.xml) and read by the update check
# to compare this build with the newest release published on GitHub.
version=${project.version}
